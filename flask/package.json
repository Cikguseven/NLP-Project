{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index_axios.html",
    "build": "parcel build index_axios.html"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "parcel-bundler": "^1.3.1"
  },
  "dependencies": {
    "parcel": "^2.5.0"
  }
}
