{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel home_node.html",
    "build": "parcel build home_node.html"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "parcel-bundler": "^1.3.1"
  }
}
